{
  "name": "backend",
  "version": "1.0.0",
  "description": "Hardhat project for the supply chain contracts",
  "private": true,
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "node": "npx hardhat node",
    "deploy": "npx hardhat run scripts/deploy.js",
    "export": "node scripts/exportArtifacts.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.6"
  }
}
